export type PaginationParams = {
    limit: number;
    offset: number;
};

export type YearTypeParams = { year: string; type: string };
export type IdParams = { id: string };
export type PrizeParams = { campaignId: string; prizeId: string };
